@{
  Layout = "_Layout";
}

@using PierresTreats.Controllers
<hr />
<h2>Welcome to Treat Box</h2>
<p>A site where you can Add and organize your favorite treats.</p>
<hr />
@if (User.Identity.IsAuthenticated)
{
  
}
else
{
  <form action="Account/Login" method="get">
  <button type="submit" class="btn">Log In</button>
  </form>
  <br>
  <p>Not a member? Register @Html.ActionLink("here", "Register")</p>
}

@if (User.Identity.IsAuthenticated)
{
  <p>Currently logged in using email - @User.Identity.Name</p>
  <br>
  @using (Html.BeginForm("LogOff", "Account"))
  {
    <input type="submit" value="Log out" class="btn" />
  }
}